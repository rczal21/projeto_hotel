//BJPNR - Mapa de ocupação e reservas do hotel

#include <stdio.h>
#include <iostream>
void fMapaHotel();

void fMapaHotel()
{
	
	char tab[21][15]; 
	int i, j, x, y; 
	
	for(i=20; i > 0; i--)
	   for(j=1; j < 15; j++)
	       tab[i][j] = '.'; 
	
	//apresenta matriz	
    system("cls");   //para limpar a tela antes de apresnetar a matriz
    printf("Apto ->\t\t");
    
    for(j = 1; j < 15; j++)
    	
        printf("%2d ", j); 
    printf("\n\n");
	   	   
   for(i = 20; i > 0 ; i--) {
   	    printf("Andar %2d\t", i); 
   	    
	   for( j=1; j < 15; j++) 
	       printf("%2c ", tab[i][j]);
	       printf("\n");
	       //fim da matriz
	}

}
int main()
{
	fMapaHotel();
}
		
	

