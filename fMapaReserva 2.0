//BJPNR - Mapa de ocupação e reservas do hotel

#include <stdio.h>
#include <iostream>
void fMapaReserva(int a, int b);



		printf("\n===========================================\n");
		printf("\tRESERVA DE APARTAMENTO\n");
		printf("===========================================\n\n");
				
		printf("Digite o numero do andar (de 1 a 20, enter para voltar): ");
		scanf("%d", &x);
		
		printf("Digite o numero do apartamento (de 1 a 14): ");
		scanf("%d", &y);
		printf("\n\n");


void fMapaReserva(int a, int b)
{
		
		if (a == '\0') //retorna para o menu
			break;
			
			if (b<= 0 || a<=0 || a>14 || b>20)
		{
			printf("Digite um apartamento valido.\n");
			system ("pause");
			continue;
		}
		tab[a][b] = 'R'; // marca a reserva
		
}
